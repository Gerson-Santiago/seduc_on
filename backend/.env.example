# backend/.env.example
# --- Configurações de AMBIENTE ---
# Opções: development, preview, production
NODE_ENV=development

# Porta do Servidor
# Dev: 3001 | Preview/Prod: 3000
PORT=3001

# --- Configurações de CORS e Frontend ---
# URL onde o frontend está rodando (sem barra no final)
FRONTEND_URL=http://localhost:5173

# Origens permitidas para requisições (separadas por vírgula se houver mais de uma)
ALLOWED_ORIGINS=http://localhost:5173

# URI de redirecionamento configurada no Google Cloud Console
GOOGLE_REDIRECT_URI=http://localhost:5173/aee/auth/callback

# --- Integração Google ---
# Client ID obtido no Google Cloud Platform
GOOGLE_CLIENT_ID=insira_seu_client_id_aqui

# --- Banco de Dados ---
# String de conexão do PostgreSQL (Prisma)
# Formato: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://usuario:senha@localhost:5432/nome_do_banco"

# --- Configurações da Aplicação ---
# Email e Nome para o primeiro acesso administrativo
SUPERADMIN_EMAIL=admin@exemplo.com.br
SUPERADMIN_NAME="Nome do Administrador"

# --- Segurança (JWT) ---
# O QUE É: Chave secreta única usada para assinar (criptografar) os tokens de sessão.
# POR QUE ESTÁ AQUI: Sem ela, o backend não consegue criar tokens seguros e o login falha.
# QUEM USA: O Backend (biblioteca jsonwebtoken) usa para validar se quem está logado é realmente quem diz ser.
# COMO CONFIGURAR: Gere uma string longa e aleatória (ex: hash SHA-256 ou frase secreta).
# IMPORTANTE: Mantenha diferente para Dev e Prod. Nunca commite o valor real no Git!
JWT_SECRET=seu_segredo_jwt_super_seguro_aqui_123

# --- Configurações de SED ---
# URL de validação do SED
# SED
LOGIN_AUTH_SED=login
SED_AUTH=auth
# URLBASE
URL_VALIDASED=https://homologacaointegracaosed.educacao.sp.gov.br/ncaapi/api
